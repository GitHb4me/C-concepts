same as if else
  
  if(statement 1 & statement 2 | statement 3)
  Block a;
  else
  Block b;
               // in ternary terms  ? : ;
    (statemen 1 & statement 2 | statement 3) ? Block a : Block b;   // braces {} does not support inside ternary operator means only one  " block "  execute at once
