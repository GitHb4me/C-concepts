#include<bits/stdc++.h>
using std::cout;
int main() {
    int x=5;
    int s=x++ + ++x;
std::cout<<s;
return 0; //pre (increase then use) and post (use then increase) increment operator concept
}
